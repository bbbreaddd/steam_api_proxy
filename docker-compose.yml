services:
  achievement-watcher-api:
    build: .
    container_name: achievement-watcher-api
    restart: unless-stopped
    ports:
      - "3888:80"
    volumes:
      - ${DATA_PATH}/steam/data:/app/data
    environment:
      - NODE_ENV=production
